import React from 'react';
import PropTypes from 'prop-types';

const Memory = props => {
    return (
        <div>
            inside memory ui...
        </div>
    );
};

Memory.propTypes = {

};

export default Memory;
