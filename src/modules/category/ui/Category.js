import React from 'react';
import PropTypes from 'prop-types';

const Category = props => {
    return (
        <div>
            inside category ui
        </div>
    );
};

Category.propTypes = {

};

export default Category;
